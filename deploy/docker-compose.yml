# File mantenuto solo come promemoria storico.
# La sorgente canonica del progetto è `../docker-compose.yml`.
# Usare i comandi standard dalla root della repository, senza `-f`:
#   docker compose up -d --build
#   docker compose ps
#   docker compose logs -f node1
#   docker compose down
